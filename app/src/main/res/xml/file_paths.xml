<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Internal app storage for backup files -->
    <files-path
        name="backup_files"
        path="backups/" />

    <!-- Cache directory for temporary files -->
    <cache-path
        name="cache_files"
        path="." />

    <!-- External files directory (if needed for sharing) -->
    <external-files-path
        name="external_backup_files"
        path="backups/" />
</paths>